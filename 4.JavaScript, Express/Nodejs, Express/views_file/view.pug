doctype html
html
    head
        meta(charset='utf-8')
    body
        h1
            a(href='/topic') Server Side JavaScript
        ul
            each topic in topics
                li
                    a(href='/topic/'+topic)=topic
        button(onclick="window.location.href='/topic/new'") new
        article
            h2= title
            = description